create schema TENANT_ADMIN;
use TENANT_ADMIN;
create table tenant(id bigint auto_increment, name varchar(255), tenant_type varchar(255));

-- create schema TENANT_1;
-- use TENANT_1;
-- create table location(id bigint auto_increment, name varchar(255));
--
-- create schema TENANT_2;
-- use TENANT_2;
-- create table location(id bigint auto_increment, name varchar(255));

